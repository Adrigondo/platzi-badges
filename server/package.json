{
  "name": "platzi-badges-api",
  "version": "0.2.0",
  "private": false,
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port ${PORT:-8888} --watch db.json",
    "seed": "node server/seed.js"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
